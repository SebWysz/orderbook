# orderbook

### Commands to Remember lol
```bash
cmake --preset debug
cmake --build --preset debug
ctest --preset test-debug
```

```bash
cmake --preset release
cmake --build --preset release
ctest --preset test-release
```

```bash
make debug
make build
make test
make run
```

```bash
cmake --list-presets
cmake --build --list-presets
ctest --list-presets
```
